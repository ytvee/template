/**
 * Methods placed in this object act as a controller between user and AudioEditor (our audio model). The user should be able to call them directly.
 */
export const controllerMutations = {
  test() {},
};
export const controllerActions = {
  test() {},
};
